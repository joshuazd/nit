version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      check-blank: false
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
